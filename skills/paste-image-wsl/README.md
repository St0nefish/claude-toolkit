# paste-image-wsl

Paste images from the Windows clipboard to disk when running in WSL.

## Usage

1. Copy an image to your clipboard in Windows
2. Run in WSL terminal:
   ```bash
   paste-image
   ```
3. Image is saved to current directory with timestamp filename