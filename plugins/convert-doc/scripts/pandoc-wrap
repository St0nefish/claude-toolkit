#!/usr/bin/env bash
set -euo pipefail
if ! command -v pandoc >/dev/null 2>&1; then
  echo "pandoc not found. Install: brew install pandoc (macOS) or apt install pandoc (Linux)" >&2; exit 1
fi
exec pandoc "$@"
