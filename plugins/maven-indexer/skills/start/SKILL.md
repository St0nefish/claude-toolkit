---
name: maven-indexer-start
description: >-
  Start the maven-indexer Docker Compose stack. Run this before using the
  maven-indexer MCP server tools (search_classes, get_class_details, etc.).
disable-model-invocation: true
allowed-tools: Bash
---

# Start Maven Indexer

Start the Docker Compose stack for the maven-indexer MCP server.

```bash
bash ${CLAUDE_PLUGIN_ROOT}/scripts/setup.sh
```
