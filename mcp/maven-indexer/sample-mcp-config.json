{
  "_comment": [
    "Sample MCP config for maven-indexer with Claude Code.",
    "Copy the mcpServers entry into your project or user config.",
    "",
    "Option A: Add to a project's .claude/settings.local.json under 'mcpServers'",
    "Option B: Add globally via: claude mcp add maven-indexer ...",
    "Option C: Add to ~/.claude/settings.json under 'mcpServers'",
    "",
    "Requires `docker compose up -d` first."
  ],

  "mcpServers": {
    "maven-indexer": {
      "command": "docker",
      "args": ["exec", "-i", "mcp-maven-indexer", "npx", "-y", "maven-indexer-mcp@latest"],
      "env": {}
    }
  }
}
